{% extends 'admin/base_admin.html.twig' %}

{% block title %}Modifier candidature{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="fs-3 mb-1">Modifier la candidature</h1>
            <p class="mb-0 text-secondary">Mise a jour de la candidature #{{ candidature.id }}.</p>
        </div>
    </div>
    <div class="card">
        <div class="card-body p-4">
            {% include 'admin/candidature/_form.html.twig' with {form: form, button_label: 'Mettre a jour'} %}
        </div>
    </div>
</div>
{% endblock %}
